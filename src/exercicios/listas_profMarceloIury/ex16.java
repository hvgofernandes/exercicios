package exercicios.listas_profMarceloIury;


public class ex16 {
    public static void main(String[] args) {
        // Vamos tentar fazer esse depois! Vi a resolução via IA e não consegueria fazer por conta própria







        /*
        import java.util.*;

class Votacao {
    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);
        int votosX = 0, votosY = 0, votosZ = 0, votosNulo = 0, votosBranco = 0;
        int totalVotos = 0;

        System.out.println("Digite os votos (X, Y, Z para candidatos; N para nulo; B para branco; F para finalizar):");

        while (true) {
            String voto = scanner.next().toUpperCase();

            if (voto.equals("F")) {
                break;
            }

            switch (voto) {
                case "X": votosX++; break;
                case "Y": votosY++; break;
                case "Z": votosZ++; break;
                case "N": votosNulo++; break;
                case "B": votosBranco++; break;
                default: System.out.println("Voto inválido! Tente novamente."); continue;
            }
            totalVotos++;
        }
        scanner.close();

        System.out.println("\nResultado da eleição:");
        System.out.printf("X: %d votos (%.2f%%)\n", votosX, (votosX / (double) totalVotos) * 100);
        System.out.printf("Y: %d votos (%.2f%%)\n", votosY, (votosY / (double) totalVotos) * 100);
        System.out.printf("Z: %d votos (%.2f%%)\n", votosZ, (votosZ / (double) totalVotos) * 100);
        System.out.printf("Nulo: %d votos (%.2f%%)\n", votosNulo, (votosNulo / (double) totalVotos) * 100);
        System.out.printf("Branco: %d votos (%.2f%%)\n", votosBranco, (votosBranco / (double) totalVotos) * 100);
    }
}
         */
    }
}
