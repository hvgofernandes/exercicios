algoritmo "soma com procedimento"

var
   x, y : inteiro
procedimento soma (A, B : inteiro)
inicio
      Escreval("Valor ", A)
      Escreval("Valor ", B)
      Escreval("Soma dos valores ", A + B)
fimprocedimento

inicio
      X <- 10
      Y <- 5
      Soma(X, Y)
fimalgoritmo
