algoritmo "soma"

var
   x, y, soma: inteiro
inicio
      escreval("Digite o valor de um número: ")
      leia(x)
      escreval("Digite o valor de outro número: ")
      leia(y)
      soma <- x + y
      escreval("A soma de ", x, " + ", y, " é igual a ", soma)
fimalgoritmo
